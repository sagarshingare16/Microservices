spring:
  application:
    name: Application-02
  cloud:
    compatibility-verifier:
      enabled: false
  boot:
    admin:
      client:
        url: http://localhost:8080/
server:
  port: 8182
management:
  endpoints:
    web:
      exposure:
        include: '*'